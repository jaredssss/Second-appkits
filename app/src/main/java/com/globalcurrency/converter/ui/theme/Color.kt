package com.globalcurrency.converter.ui.theme

import androidx.compose.ui.graphics.Color

// ── Primary palette ──────────────────────────────────────────────────────────
val GoldPrimary       = Color(0xFFFFD700)  // Gold – money / currency
val GoldLight         = Color(0xFFFFE84D)
val GoldDark          = Color(0xFFBFA000)

// ── Backgrounds (pure-dark) ──────────────────────────────────────────────────
val DarkBackground    = Color(0xFF0A0A0A)  // near-black page background
val DarkSurface       = Color(0xFF141414)  // card / sheet surface
val DarkSurfaceVariant= Color(0xFF1E1E1E)  // elevated surface
val DarkUtilitySurface= Color(0xFF2A2A2A)  // utility keys surface
val DarkDangerSurface = Color(0xFF2A1010)  // destructive key surface
val DarkOutline       = Color(0xFF2C2C2C)  // dividers / borders

// ── Secondary / accent ───────────────────────────────────────────────────────
val EmeraldAccent     = Color(0xFF00C853)  // positive / profit green
val CrimsonAccent     = Color(0xFFD50000)  // negative / loss red
val SkyAccent         = Color(0xFF40C4FF)  // info / links

// ── On-colors ────────────────────────────────────────────────────────────────
val OnDarkPrimary     = Color(0xFF1A1A00)  // text on gold buttons
val OnDarkBackground  = Color(0xFFEEEEEE)  // body text
val OnDarkSurface     = Color(0xFFCCCCCC)  // secondary text
val OnDarkDisabled    = Color(0xFF666666)  // disabled / hint text

// ── Premium badge ────────────────────────────────────────────────────────────
val PremiumGradientStart = Color(0xFFFFD700)
val PremiumGradientEnd   = Color(0xFFFF8C00)
